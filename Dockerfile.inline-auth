# Build from the authenticated image available on Docker Hub
FROM pridhvikrishna/tiny-backspace-claude:latest

# Set working directory
WORKDIR /workspace

# Ensure Claude is accessible
ENV PATH="/usr/local/bin:${PATH}"

# Switch to non-root user if available
USER claude-user

# Default command
CMD ["claude", "--version"]