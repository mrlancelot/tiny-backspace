# Use the locally authenticated Claude image
FROM tiny-backspace-claude:authenticated

# Set working directory
WORKDIR /workspace

# Ensure Claude is accessible in PATH
ENV PATH="/usr/local/bin:${PATH}"

# Default command
CMD ["claude", "--version"]